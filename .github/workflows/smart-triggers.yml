name: smart-triggers

on:
  pull_request:
    branches:
      - main

jobs:
  smart-triggers:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Pull request triggered successfully"
