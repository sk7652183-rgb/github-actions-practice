name: call-build.yml
on: 
  push:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/reusable-build.yml
    with:
      app-name: "Web-app"
      environment: "production"
    secrets: 
      docker_token: ${{ secrets.DOCKERHUB_TOKEN }}
  
